# Web 框架
fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.5  # 文件上传依赖

# 向量数据库
chromadb>=0.4.22

# LLM / Embedding API
openai>=1.12

# 配置与环境变量
python-dotenv>=1.0

# 结构化日志
loguru>=0.7.0

# 数据校验
pydantic>=2.0

# 测试（开发依赖）
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# 代码质量（开发依赖）
black>=24.0
isort>=5.13
flake8>=7.0

# 文档处理（PDF / Word / Excel / CSV）
pandas>=2.0
pypdf>=5.0          # PDF 解析（默认，纯 Python，兼容性好）
python-docx>=1.1.0

# 可选：更高效的 PDF 解析（需要系统依赖，部分 Python 版本可能不支持）
# PyMuPDF>=1.23.0

# 可选：本地 Embedding 模型
sentence-transformers>=2.3
